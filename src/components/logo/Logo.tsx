import React from "react";
import logo from "./logo.svg";

export const Logo = () => {
    return (
        <div className="page-logo">
            <img src={logo} alt="Splitter" />
        </div>
    );
};
